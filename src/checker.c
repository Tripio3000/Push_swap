#include "../includes/push_swap.h"

int 	main(int ac, char **av)
{
	char *arr;
	while(get_next_line(0, &arr))
	{
		if (ft_strcmp(arr, "sa"))
			printf("Helloworld");
	}
//	printf("Helloworld");
	return (0);
}